{
  "build": {
    "steps": [
      "./gradlew clean build -x test -x check"
    ]
  },
  "start": {
    "command": "java",
    "args": [
      "$JAVA_OPTS",
      "-jar",
      "-Dserver.port=$PORT",
      "build/libs/All-API.jar"
    ]
  }
}
